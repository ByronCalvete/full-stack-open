{
  "name": "coursedata",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../part2/coursedata && pnpm build && cp -r dist ../../part3/coursedata",
    "deploy:full": "pnpm build:ui && git add . && git commit -m uibuild --no-verify && git push"
  },
  "keywords": [],
  "author": "Byron J Calvete",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
