{
  "name": "coursedata",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon --env-file=config.env index.js",
    "build:ui": "rm -rf dist && cd ../../part2/v2/coursedata && pnpm run build && cp -r dist ../../../part4/coursedata",
    "lint": "eslint .",
    "test": "jest --verbose"
  },
  "keywords": [],
  "author": "Byron Calvete",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.0.3"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
